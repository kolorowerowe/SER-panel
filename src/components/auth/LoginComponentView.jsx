import React from 'react';

const LoginComponentView = () => {
    return (
        <div>
            login
        </div>
    );
};

LoginComponentView.propTypes = {

};

export default LoginComponentView;
